apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
  namespace: keycloak
spec:
  secretName: my-tls-secret
  issuerRef:
    name: my-ca-issuer
    kind: ClusterIssuer
  dnsNames:
	  # - keycloak-ai-platforms-service-keycloak.apps-crc.testing
    # - keycloak-ai-platforms-service.keycloak.svc.cluster.local